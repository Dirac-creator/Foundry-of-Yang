# 协作规范

## 日常流程

1. 创建 Issue：明确输入、输出、适用条件和验收标准。
2. 同步主线：`git switch main`，然后 `git pull --ff-only`。
3. 创建短期分支，例如 `git switch -c feature/etching-import`。
4. 实现一项功能，同时补充必要的测试、示例和文档。
5. 执行 README 中的校验命令与测试。
6. `git add` 指定文件，检查 `git diff --cached`，再 commit 和 push。
7. 提交 PR，由至少一位其他成员审核后合并。

维护者应在 GitHub 设置主线保护与必需检查（以仓库套餐支持为准）。本地文件不会自动启用这些规则。初始框架可以作为首次主线提交，后续改动通过 PR。

## 共同约定

- 文件 UTF-8；Python 函数、字段使用英文 snake_case；文档可用中文。
- 数据字段、单位或接口改变时同步更新 Schema、文档、示例和测试。
- 不擅自重命名公共编号；模型校准生成新版本，不覆盖旧预测。
- 模型测试包含适用范围和独立验证数据，运行成功不代表物理正确。
- 不提交真实大数据、虚拟环境、凭据、生成结果。
- 模块负责人由课题组在 Issue 中指定，确定 GitHub 用户名后再添加 CODEOWNERS。

## 提交身份

使用可识别的作者名称和 GitHub 已验证邮箱（或 GitHub noreply 邮箱）。每个成员使用自己的账号。
